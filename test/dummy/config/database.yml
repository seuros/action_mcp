development:
  primary:
    adapter: postgresql
    database: actionmcp_development
    username: postgres
    password: postgres
    host: localhost

test:
  primary:
    adapter: postgresql
    database: actionmcp_test
    username: postgres
    password: postgres
    host: localhost