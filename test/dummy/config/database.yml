development:
  primary:
    url: <%= ENV.fetch("DATABASE_URL", "postgresql://ubuntu:password@localhost/actionmcp_development") %>

test:
  primary:
    url: <%= ENV.fetch("DATABASE_URL", "postgresql://ubuntu:password@localhost/actionmcp_test") %>
