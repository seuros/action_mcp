development:
  primary:
    adapter: postgresql
    database: actionmcp_development
    url: <%= ENV.fetch("DATABASE_URL", "postgresql://ubuntu:password@localhost") %>

test:
  primary:
    adapter: postgresql
    database: actionmcp_test
    url: <%= ENV.fetch("DATABASE_URL", "postgresql://ubuntu:password@localhost") %>
