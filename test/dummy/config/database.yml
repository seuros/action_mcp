development:
  primary:
    url: <%= ENV.fetch("DATABASE_URL", "sqlite3:db/development.sqlite3") %>
    database: action_mcp_dev

test:
  primary:
    url: <%= ENV.fetch("DATABASE_URL", "sqlite3:db/test.sqlite3") %>
    database: action_mcp_test
